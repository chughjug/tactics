name: Generate LogiqBoard Link

on:
  workflow_dispatch: # Keeps the manual "Run workflow" button in GitHub
  repository_dispatch:
    types: [create_board_session] # Custom event type for your API trigger

jobs:
  create-board:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install Playwright
        run: |
          pip install playwright
          playwright install chromium

      - name: Generate and Scrape Link
        run: |
          python -c "
          from playwright.sync_api import sync_playwright

          def create_board():
              with sync_playwright() as p:
                  browser = p.chromium.launch(headless=True)
                  context = browser.new_context()
                  page = context.new_page()
                  
                  try:
                      page.goto('https://logiqboard.com/')
                      page.locator('button.create-board-btn').click()
                      page.wait_for_url('**/boards/*') 
                      
                      print('=== Board Successfully Created ===')
                      print(f'Shared Room URL: {page.url}')
                      
                  except Exception as e:
                      print(f'Error occurred: {e}')
                  finally:
                      context.close()
                      browser.close()

          if __name__ == '__main__':
              create_board()
          "
